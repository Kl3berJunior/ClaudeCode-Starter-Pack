# Agent Sessions

Este diretorio recebe relatorios criados por `/close-session`.

Regras do starter pack:

- nao distribuir sessoes datadas no template
- criar arquivos no formato `YYYY-MM-DD-session.md` apenas durante uso real
- deixar o historico nascer do workspace adotado, nao do repositorio de origem
